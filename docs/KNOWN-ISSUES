.: $Id$ :.

The following is a list of known issues for the current release of
CoSMIC. The issues that appear on this list will be resolved in the
next release of CoSMIC.

. In the property manager window, for cases of at least long long
  integers, we are unable to select the box to put in a value.

  WORKAROUND: This has been resolved in the nightly release.

. Creating an instance of a component assembly in another component
  assembly is prohibited by the PICML Model Intelligence.

  WORKAROUND: This has been resolved in the nightly release.

. Using the -i option on idl_to_picml can result in duplicate id
  generation on very large models.

  WORKAROUND: This has been resolved in the nightly release.
