/// $Id$

workspace (GAME) {
  cmdline += -include $GAME_ROOT/MPC/config

  exclude (!prop:windows) {
    cmdline += -include $GAME_ROOT/contrib/CCF/MPC/config \
               -base ccf_root

    game

    exclude (!prop:microsoft, wix) {
      cmdline += -static \

      $(GAME_ROOT)/contrib/CCF/CCF/CodeGenerationKit
    }
  }
  else {
    game/xml
    game/xme
  }

  $(ACE_ROOT)/ace/ace.mpc
}
