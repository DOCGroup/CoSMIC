/// $Id$

workspace (GAME) {
  cmdline += -include $COSMIC_ROOT/MPC/config

  exclude (!prop:windows) {
    // == visit all the projects

    ./game
  }
  else {
    // == non-Windows can only visit the XME project

    ./game/xme
  }
}
