// $Id$

workspace : picml {
  //WSML interpreters are commented temporarily because many mcpp related 
  //changes need to go in to fix them.
  //PIM/WSML/interpreters/Interpreters.mwc
  PIM/SIML/interpreters/Interpreters.mwc

  PSM/CQML/interpreters/Interpreters.mwc
  PSM/DQML

  static {
    cmdline += -static \
               -include $COSMIC_ROOT/contrib/CCF/MPC/config

    $(COSMIC_ROOT)/contrib/CCF/CCF/CodeGenerationKit
  }
}
