// $Id$

workspace {
  cmdline += -feature_file $COSMIC_ROOT/default.features \
             -include $COSMIC_ROOT/MPC/config

  specific (prop:microsoft) {
    OmnetInterpreter
    AspectInterpreter
    ConfiguratorInterpreter
  }

  exclude {
    BenchmarkInterpreter/benchmark_test
  }
}