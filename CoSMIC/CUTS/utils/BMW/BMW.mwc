// $Id$

workspace (BMW) {
  // web application (i.e., webapp = 1)
  BMW.mpc

  csharp {
    // process the following directories using C#
    // as the target language.
    cmdline += -language csharp

    Database_Utility
  }
}
