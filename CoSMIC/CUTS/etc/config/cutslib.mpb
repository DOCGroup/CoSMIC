// $Id$

project : acelib, cuts_base {
  libs  += CUTS
  after += CUTS

  libout = $(CUTS_ROOT)/lib

  specific (vc6, vc7, vc71, vc8, nmake) {
    dllout = $(CUTS_ROOT)/bin
  } else {
    dllout = $(CUTS_ROOT)/lib
  }
}
