// $Id$

workspace (tests) {
  cmdline += -include $CUTS_ROOT/etc/config \
             -include $CUTS_ROOT/tests/etc/config

  // -- explicitly specify the project directories
  tests.mpc
  cuts
}
