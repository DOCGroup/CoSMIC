// $Id$

project (PICML_CIAO_Impl) : cutslib, udm_component, gme_interpreter {
  sharedname = CUTS_PICML_CIAO_Impl

  // -- dependencies
  after -= CUTS ACE
  after += CUTS_UDM_CIAO

  libs  -= CUTS ACE
  libs  += CUTS_UDM_CIAO

  libpaths -= $(ACE_ROOT)/lib
  includes -= $(ACE_ROOT)

  // -- required features
  requires += boost

  Source_Files {

  }
}
