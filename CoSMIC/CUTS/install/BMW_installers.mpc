// $Id$

project (BMW) : wix {
  prebuild = \
    aspnet_compiler -v CUTS -p $(CUTS_ROOT)/utils/BMW -f \
    -u -fixednames $(CUTS_ROOT)/bin/BMW

  specific {
    wix_flags += -ext WixUIExtension -ext WixIisExtension
    light_flags += -ext WixUIExtension -ext WixIisExtension -cultures:en-us
  }

  Wxs_Files {
    BMW.wxs
  }
}
