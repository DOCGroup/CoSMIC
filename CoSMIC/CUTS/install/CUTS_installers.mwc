// $Id$

workspace (CUTS_installers) {
  cmdline += -include $CUTS_ROOT/etc/config

  exclude (!vc71, !vc8) {
    CUTS_CoSMIC_installers.mpc
  }

  exclude (!vc8) {
    BMW_installers.mpc
  }
}
