// $Id$

workspace (installers) {
  cmdline += -include $COSMIC_ROOT/MPC/config

  exclude (!vc71, !vc8) {
    CUTS_CoSMIC_installers.mpc
  }

  exclude (!vc8) {
    BMW_installers.mpc
  }
}
