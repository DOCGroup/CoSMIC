// $Id$

project : udm, cosmic_utils {
  after    += PICML
  libs     += PICML

  includes += $(COSMIC_ROOT)/PIM/PICML/interpreters

  macros       += UDM_DYNAMIC_LINKING
  dynamicflags += PICML_HAS_DLL


  specific (wix) {
    install_location = bin
  }
}
